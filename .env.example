# Application Environment
NODE_ENV=development

# Server Port (macOS users: use 3000 instead of 5000 due to AirPlay)
PORT=3000

# Anthropic API Keys (for cost tracking across dev/prod)
NAVIETTA_DEV_API_KEY=your_anthropic_dev_api_key_here
NAVIETTA_PROD_API_KEY=your_anthropic_prod_api_key_here
# Fallback (if NAVIETTA_PROD_API_KEY not set)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LangSmith Configuration (Development Only)
# Set to "true" to enable LangSmith tracing in development
LANGSMITH_TRACING=true
# Get your API key from https://smith.langchain.com/settings
LANGSMITH_API_KEY=your_langsmith_api_key_here
# Optional: Set a custom project name (defaults to "default")
LANGSMITH_PROJECT=navietta-dev
# Optional: LangSmith endpoint (usually not needed)
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com

# Database Configuration (if using Neon)
DATABASE_URL=your_neon_database_url_here

# Replit Deployment Detection (automatically set by Replit)
# REPLIT_DEPLOYMENT=1
